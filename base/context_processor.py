#TODO Page title needed? from .models import Page
# from django.conf import settings

# def published_pages(request):
#     pages = Page.objects.filter(is_published=True)
#     return {'published_pages': pages}
