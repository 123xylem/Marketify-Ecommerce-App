{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
      "builder": "NIXPACKS",
      "buildCommand": "apt-get update && apt-get install -y libmagic1 && python manage.py migrate && python manage.py collectstatic --noinput",
    "watchPatterns": [
      "/backend/**/*"
    ],
    "nixpacksConfigPath": "backend/nixpacks.toml"
  },
  "deploy": {
    "runtime": "V2",
    "numReplicas": 1,
    "startCommand": "python manage.py runserver 0.0.0.0:8000",
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
   "builds": [
    {
      "env": "python",
      "build": {
        "command": "apt-get update && apt-get install -y libmagic1 && python manage.py migrate && python manage.py collectstatic --noinput"
      }
    }
  ],
  
  "env": {
    "LD_LIBRARY_PATH": "/usr/local/lib:$LD_LIBRARY_PATH"
  }


}
